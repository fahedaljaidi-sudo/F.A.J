# ✅ تحديث معلومات المدير - دليل التنفيذ

## 📋 معلومات المدير الجديدة

تم تحديث ملف `backend/database/init.js` ليحتوي على:

```
اسم المستخدم: admin
الاسم الكامل: فهد الجعيدي
الرتبة: مدير النظام (admin)
كلمة المرور: admin@123
```

---

## 🔄 خطوات تطبيق التحديثات

### الطريقة 1: إعادة نشر Backend على Railway (موصى بها)

1. **ارفع التغييرات إلى GitHub:**
```bash
cd c:\Users\gamin\FAJ
git add .
git commit -m "Update admin credentials: فهد الجعيدي / admin@123"
git push
```

2. **اذهب إلى Railway Dashboard:**
   - رابط: https://railway.app/dashboard
   - افتح مشروع FAJ

3. **أعد نشر Backend:**
   - اضغط على "Deployments"
   - اضغط على "Deploy" أو "Redeploy"
   - انتظر 2-3 دقائق

4. **افتح Terminal في Railway:**
   - اضغط على Terminal
   - اكتب:
   ```bash
   cd backend
   rm -f database/security.db
   node database/init.js
   pm2 restart all
   ```

5. **جاهز!** الآن يمكنك تسجيل الدخول بـ:
   - Username: `admin`
   - Password: `admin@123`

---

### الطريقة 2: تحديث البيانات مباشرة (للمستخدمين المتقدمين)

في Railway Terminal:

```bash
cd backend
sqlite3 database/security.db
```

ثم اكتب:

```sql
-- تحديث الاسم
UPDATE users 
SET full_name = 'فهد الجعيدي' 
WHERE username = 'admin';

-- عرض كلمة المرور المشفرة الجديدة
SELECT '$2a$10$your_new_hash_here';

-- تحديث كلمة المرور (استبدل الـ hash)
UPDATE users 
SET password_hash = '$2a$10$...' 
WHERE username = 'admin';

-- التحقق من التحديث
SELECT username, full_name, role FROM users WHERE username = 'admin';

.quit
```

**ملاحظة:** للحصول على hash كلمة المرور الجديدة:
```bash
node -e "console.log(require('bcryptjs').hashSync('admin@123', 10))"
```

---

### الطريقة 3: حذف قاعدة البيانات وإعادة إنشائها (سيحذف جميع البيانات!)

**⚠️ تحذير: هذا سيحذف جميع الزوار والجولات والبيانات!**

```bash
cd backend
rm -f database/security.db
node database/init.js
pm2 restart all
```

---

## 🧪 اختبار التحديثات

1. افتح: https://f-a-j.vercel.app
2. سجل دخول بـ:
   - Username: `admin`
   - Password: `admin@123`
3. يجب أن ترى:
   - الاسم: **فهد الجعيدي**
   - الرتبة: **مدير النظام**

---

## 🔐 أمان كلمة المرور

كلمة المرور الجديدة `admin@123` مخزنة بشكل مشفر باستخدام bcrypt:
- الخوارزمية: bcrypt
- الـ rounds: 10
- Hash مخزن في قاعدة البيانات (غير قابل للعكس)

**لا يمكن لأحد معرفة كلمة المرور الأصلية من قاعدة البيانات!**

---

## 📝 ملاحظات مهمة

### 1. البيانات الحالية
إذا اخترت الطريقة 3 (حذف قاعدة البيانات):
- ✅ ستحصل على قاعدة بيانات جديدة نظيفة
- ❌ ستفقد جميع الزوار والجولات المسجلة
- ✅ المستخدم admin سيكون بالمعلومات الجديدة

### 2. النسخ الاحتياطي
قبل حذف قاعدة البيانات، قم بعمل نسخة احتياطية:
```bash
cp database/security.db database/backup_$(date +%Y%m%d).db
```

### 3. استعادة النسخة الاحتياطية
إذا أردت استعادة البيانات القديمة:
```bash
cp database/backup_20260123.db database/security.db
pm2 restart all
```

---

## ✅ قائمة التحقق

بعد تطبيق التحديثات، تأكد من:

- [ ] تم رفع التغييرات إلى GitHub
- [ ] تم إعادة نشر Backend على Railway
- [ ] تم حذف قاعدة البيانات القديمة (إذا لزم الأمر)
- [ ] تم إنشاء قاعدة البيانات الجديدة
- [ ] تم إعادة تشغيل الخدمة (pm2 restart all)
- [ ] اختبار تسجيل الدخول بـ admin / admin@123
- [ ] التحقق من ظهور "فهد الجعيدي" في الداشبورد

---

## 🎯 الخلاصة

الآن معلومات المدير محدثة في الكود. لكن لتطبيقها على قاعدة البيانات الفعلية:

**الحل الأبسط:**
1. ارفع إلى GitHub: `git push`
2. افتح Railway Terminal
3. احذف قاعدة البيانات القديمة: `rm -f database/security.db`
4. أنشئ قاعدة بيانات جديدة: `node database/init.js`
5. أعد تشغيل الخدمة: `pm2 restart all`
6. سجل دخول بـ: `admin / admin@123`

**جاهز!** ✅

---

**آخر تحديث:** 23 يناير 2026  
**الحالة:** ✅ جاهز للتطبيق
